language: bash
services: docker

script:
  - /bin/true

after_deploy:
  - echo "Deploying..."
    

deploy:
  - provider: script
    script: /bin/true
    on:
      all_branches: true
      condition: $TRAVIS_BRANCH = master || $TRAVIS_TAG = tag
